# DM3 Long-Horizon Next-Phase Recommendation

## Exact Phase Verdict

- `phase_outcome=PASS`
- `route_outcome=FAIL`
- The preserved bundled G2 family is retired on the current callable same-binary surface.
- The blocker is no longer "which same-binary route should we try?"; it is whether the historical bundled G2 residue belongs to a neighboring launcher generation or requires explicit redevelopment.

## Recommendation

Plan the next bounded move as launcher-adjacency classification plus
redevelopment-boundary control:

1. determine whether the historical bundled `G2` residue belongs to a
   neighboring launcher generation
2. if not, write the explicit redevelopment decision instead of reopening
   same-binary archaeology
3. keep the work bounded to provenance, launcher surface recovery, and
   redevelopment classification

## Parallel Standing Governance Work

- reconcile the Genesis default-validator target across Mac, historical RM10,
  and fresh RM10 witness surfaces
- keep fresh RM10 Genesis lanes explicitly `governed_non_sovereign`
- preserve the current `prebuilt_stub` and `mixed_prebuilt_backed` labels until
  source-built parity is actually proved

## Later-Lane Status

- RM10 GPU is not ready now; it needs a different already-live CPU reference
  observable named explicitly under the governed witness floor.
- RM10 NPU is feasibility-only; only user-space reachability or a narrow
  receiptable assist role may be attempted.
- Heterogeneous embodiment is premature until single-lane CPU governance, GPU
  parity, and NPU feasibility prerequisites all exist on the same observable
  family.

## What Not To Do Next

- do not reopen same-binary bundled `G2` route hunting
- do not widen into source reconstruction or modern substitute code without
  explicitly calling it redevelopment
- do not let witness-floor governance cleanup masquerade as routed recovery
- do not promote smoke, strings residue, hardware presence, or prep runbooks
  into present-tense readiness
