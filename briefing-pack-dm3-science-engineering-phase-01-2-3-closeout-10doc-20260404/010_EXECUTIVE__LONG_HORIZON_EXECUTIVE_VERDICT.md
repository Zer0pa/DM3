# DM3 Long-Horizon Executive Verdict

## Exact Phase Verdict

- `phase_outcome=PASS`
- `route_outcome=FAIL`
- The preserved bundled G2 family is retired on the current callable same-binary surface.
- The blocker is no longer "which same-binary route should we try?"; it is whether the historical bundled G2 residue belongs to a neighboring launcher generation or requires explicit redevelopment.

## What is definitely true now

- The 3D Double Meru remains source-backed through the fresh geometry probe.
- Mac Genesis remains the only current `sovereign` witness lane.
- Historical RM10 Genesis parity remains `comparison_only`, not present-tense
  sovereignty.
- Fresh RM10 SoC and raw-workspace Genesis lanes remain
  `governed_non_sovereign`, with unresolved default-validator reconciliation
  and no source-built parity promotion.
- Root and bundled hybrid runners remain `archaeology_only` residue surfaces.
- Phase `01.2.3` succeeded at the honest narrowing task because it closed the
  same-binary route question exactly rather than softening it.

## What is still blocked

- The blocker is no longer "which same-binary route should we try?"; it is
  whether the historical bundled G2 residue belongs to a neighboring launcher
  generation or requires explicit redevelopment.
- The authoritative `verify.json` target is still unresolved across the fresh
  Mac lane, the historical RM10 parity lane, and the newer phone-local lanes.
- Fresh RM10 witness lanes still sit below source-built parity.

## What has been falsified

- Another same-binary route sweep is not the honest next move.
- Smoke, strings residue, help text, wrapper residue, or hardware presence do
  not count as routed recovery.
- Fresh phone executability does not promote RM10 into sovereign authority.
- GPU, NPU, and heterogeneous possibility do not outrank the governed witness
  floor.

## Later-Lane Ceiling

- RM10 GPU is not ready now; it remains an `engineering_only` lane until a
  different already-live CPU reference observable is named explicitly under the
  governed witness floor.
- RM10 NPU remains `feasibility_only`; only user-space reachability or a narrow
  receiptable assist role is admissible.
- Heterogeneous embodiment remains premature until a single-lane CPU reference
  observable is already live, GPU parity has cleared on that same observable,
  and NPU feasibility is already bounded.

## Next Bounded Move

- Open a bounded launcher-adjacency or redevelopment-decision phase for the
  historical bundled `G2` residue.
- Keep default-validator reconciliation and source-build classification as
  standing parallel governance work.
- Do not reopen same-binary route hunting, GPU-first escalation, or mixed-lane
  ambition as a substitute for the closed route verdict.
