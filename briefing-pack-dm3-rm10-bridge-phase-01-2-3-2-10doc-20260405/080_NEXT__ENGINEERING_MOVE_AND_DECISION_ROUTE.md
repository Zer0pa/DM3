# Next

Single best next engineering move:

1. run one narrow outlier-localization pass on the exact same residue `F2`
   CPU/GPU harmonic family under locked identity and telemetry capture
2. keep `F1` as the governed scientific anchor while doing so
3. treat the result only as residue-family classification, not as bridge progress

Do not do next:

- do not keep replaying residue GPU rows without the specific outlier-localization question
- do not turn inventory-only NPU evidence into a programme
- do not claim heterogeneous role partition without handoff artifacts
- do not let a new pack substitute for a stronger authority metric
