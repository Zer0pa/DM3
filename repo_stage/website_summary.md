# DM3 — One-Page Summary

**DM3 is a compiled structural diagnostic on a fixed 380-vertex
C3-symmetric graph. It runs a 72,960-dimensional relaxation system,
exposes twelve callable tasks, and includes a self-evaluating six-gate
campaign (`exp_r1_r4_campaign`) that emits structured pass/fail
verdicts about its own operating regime.**

## The headline finding (Session 7 closeout)

Session 7 closed PRD v2 and promoted three front-door lines:

- **First receipted positive learning finding:** `exp_k2_scars`
  LEARNS-STRONG at `--steps 20` with `best_uplift = 1.324`, then
  overfits at `--steps 50`.
- **Dynamics-layer substrate null:** harmonic `p(HIGH)` remains inside
  overlapping Wilson confidence intervals across six Session 7 arms and
  665 total episodes.
- **2×2 gate cross-control closed:** `R1` responds to `--adj`,
  `R2` responds to `--tags`, and `RA+v2+steps=50` flips both together
  while nearly doubling `r4.transfer_ratio`.

## The gate surface, sharpened

- Session 7 smoke fixes a default gate-surface fingerprint at canonical
  SHA **`9006df4ec02c8872...`** on the `--steps 5` surface.
- Session 7 weakens the old “R3 payload moves with steps” reading:
  on the SY-default surface, requested `--steps > 20` does not extend
  `operational_steps` above `10`, and `R3` remains false.
- `T11_cross_control/` confirms that `R1` is pure `--adj` and `R2` is
  pure `--tags`.
- `S11_final/` closes the combined-axis line; the promoted claim is the
  `steps=50` receipt where `r4.transfer_ratio` rises from `1.369` to
  `2.678`.

## The task surface, sharpened

- `exp_k2_scars` is now the first promoted positive learning task.
- Session 8 confirms fixed-config `exp_k2_scars` KPI determinism, the
  sharp `49 -> 50` overfit cliff, and ARM64 cross-platform determinism
  for the tested baseline values.
- Session 8 adds scoped candidates: xnor dataset invariance,
  `RA+v2+steps=20` zero learning, a trimodal sawtooth K2 curve, and
  three `--steps`-decorative task surfaces.
- `exp_k3_truth_sensor` does real internal work
  `(108.16 → 22.29 error; 79.4% reduction)` on the Session 7 surface,
  but B3 shows `--steps` changes absolute baseline/gap values.
- `resonance_r3`, `resonance_v2`, and `exp_i2` ignore `--steps` at
  `steps 1` versus `20` on the tested default surfaces.

## Live Session 8 note

Phase A plus A5/B3/A6 are closed under a scientific-learning frame. The
original Phase A local mirror contains 52 per-run receipt/log pairs
while the final report and summaries use 55 total-run accounting; that
count seam remains visible. A5/B3/A6 add 57 local per-run receipts.
Promoted lines are fixed-config determinism (`ξ`), a sharp `49 -> 50`
overfit cliff (`ο`), and ARM64 cross-platform determinism (`τ`).
Candidate lines are scoped dataset invariance (`π`),
`RA+v2+steps=20` zero learning (`ρ`), trimodal sawtooth curve shape
(`σ″`), and three scoped `--steps`-decorative tasks (`φ`).

## What DM3 is not

- Not an AI system.
- Not a transformer-created bistability.
- Not a tunable resonance computer.
- Not a C3-asymmetric coupling.
- Not a general learning claim outside the narrow `exp_k2_scars` line.
- Not a folder for commercial framing.

## Visible seam kept visible

The Session 7 final report closes a bit-level gate-layer substrate-null
battery, but the handover names standalone `S2_pinned / S4_airplane /
S6_core` directories that are not exposed verbatim in this checkout.
Session 8 adds a second live seam: 55 total-run accounting in the final
report versus 52 local per-run receipt/log pairs. B3 also keeps κ
honest: `--steps` changes K3 baseline/gap values even though the
selected sensor error stays near 22.3. The repo therefore keeps all of
these mismatches explicit in `REPO_AGENT_FINDINGS.md`.
