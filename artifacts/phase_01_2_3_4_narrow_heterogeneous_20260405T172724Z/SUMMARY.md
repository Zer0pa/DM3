# Narrow Heterogeneous Abstain Packet

The official same-family `F2` handoff exists on the top-level
`/data/local/tmp/dm3_runner` surface, but the retained four-row packet at
`artifacts/rm10_f2_outlier_20260405T171018Z/` ended
`outcome_class=whole_session_instability`.

That means there is no preserved same-family observable that can carry a narrow
heterogeneous split honestly. This packet freezes the correct verdict:
`heterogeneous_handoff_verdict=ABSTAIN`.
