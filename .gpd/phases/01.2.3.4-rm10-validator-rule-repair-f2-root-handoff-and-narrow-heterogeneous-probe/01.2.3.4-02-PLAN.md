---
phase: 01.2.3.4-rm10-validator-rule-repair-f2-root-handoff-and-narrow-heterogeneous-probe
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/rm10_f2_root_repair_*/
  - docs/restart/F2_OUTLIER_LOCALIZATION_PLAN.md
  - tools/rm10_engineering_readiness.py
interactive: false

conventions:
  units: "dimensionless computational units unless protocol says otherwise"
  metric: "not applicable"
  coordinates: "graph / lattice coordinates with explicit boundary and bulk labeling"

dimensional_check:
  f2_root_receipt: "[dimensionless]"

approximations:
  - name: "bounded top-level root-family repair loop"
    parameter: "top-level `/data/local/tmp/dm3_runner` only"
    validity: "valid for isolating the real top-level root-family preconditions and stall envelope without widening into route archaeology on the legacy surface"
    breaks_when: "the legacy `/data/local/tmp/dm3/dm3_runner` is allowed to stand in for the top-level family"
    check: "retain every top-level experiment separately from any legacy comparison evidence"

contract:
  scope:
    question: "Can the top-level `/data/local/tmp/dm3_runner` root family be repaired to one bounded callable CPU row on its own surface, or reduced to an exact blocker without legacy substitution?"
  claims:
    - id: "claim-root-family"
      statement: "The top-level `F2` root family can be classified honestly as callable or blocked on its own surface, with its missing ambient dependencies and post-resonance stall envelope either repaired or exactly pinned down."
      deliverables: ["deliv-root-dossier", "deliv-root-packet"]
      acceptance_tests: ["test-root-family"]
      references: ["ref-context", "ref-research", "ref-surface-probe", "ref-invocation-dossier"]
  deliverables:
    - id: "deliv-root-dossier"
      kind: "report"
      path: "docs/restart/F2_OUTLIER_LOCALIZATION_PLAN.md"
      description: "Updated exact plan and verdict boundary for the top-level `F2` root family."
    - id: "deliv-root-packet"
      kind: "dataset"
      path: "artifacts/rm10_f2_root_repair_*/"
      description: "Retained top-level root-family repair or blocker packet with layout probes, hashes, and run verdicts."
  references:
    - id: "ref-context"
      kind: "prior_artifact"
      locator: ".gpd/phases/01.2.3.4-rm10-validator-rule-repair-f2-root-handoff-and-narrow-heterogeneous-probe/01.2.3.4-CONTEXT.md"
      role: "definition"
      why_it_matters: "Locks the no-legacy-substitution rule."
      applies_to: ["claim-root-family"]
      must_surface: true
      required_actions: ["read", "use"]
    - id: "ref-research"
      kind: "prior_artifact"
      locator: ".gpd/phases/01.2.3.4-rm10-validator-rule-repair-f2-root-handoff-and-narrow-heterogeneous-probe/01.2.3.4-RESEARCH.md"
      role: "definition"
      why_it_matters: "Constrains this plan to the top-level family and the next admissible same-family handoff."
      applies_to: ["claim-root-family"]
      must_surface: true
      required_actions: ["read", "use"]
    - id: "ref-surface-probe"
      kind: "prior_artifact"
      locator: "artifacts/rm10_f2_surface_probe_20260405T132732Z/"
      role: "benchmark"
      why_it_matters: "Retains the current hidden dependency and stall envelope that this plan must repair or sharpen."
      applies_to: ["claim-root-family"]
      must_surface: true
      required_actions: ["read", "compare", "use"]
    - id: "ref-invocation-dossier"
      kind: "prior_artifact"
      locator: "docs/restart/INVOCATION_SURFACE_DOSSIER.md"
      role: "comparison"
      why_it_matters: "Shows what the legacy callable surface can and cannot be used for in comparison."
      applies_to: ["claim-root-family"]
      must_surface: true
      required_actions: ["read", "compare"]
  acceptance_tests:
    - id: "test-root-family"
      subject: "claim-root-family"
      kind: "benchmark"
      procedure: "Run bounded top-level-only cleanroom, layout, and ambient-precondition experiments until the top-level root family either emits one retained callable CPU receipt or reduces to an exact blocker packet with the smallest decisive explanation."
      pass_condition: "The outcome is either `callable` on the top-level surface or an exact blocker verdict that excludes legacy substitution and names the decisive missing precondition or stall class."
      evidence_required: ["deliv-root-dossier", "deliv-root-packet", "ref-surface-probe"]
  forbidden_proxies:
    - id: "fp-legacy-substitution"
      subject: "claim-root-family"
      proxy: "Using `/data/local/tmp/dm3/dm3_runner` success as proof that the top-level `/data/local/tmp/dm3_runner` family is repaired"
      reason: "The phase question is about the top-level family specifically."
    - id: "fp-cleanroom-inflation"
      subject: "claim-root-family"
      proxy: "Treating an arbitrary copied-layout experiment as the restored canonical root family without showing what exact precondition it proved"
      reason: "A cleanroom result is only admissible if it isolates the real runtime precondition or repair."
  links:
    - id: "link-root-dossier"
      source: "claim-root-family"
      target: "deliv-root-dossier"
      relation: "supports"
      verified_by: ["test-root-family"]
    - id: "link-root-packet"
      source: "claim-root-family"
      target: "deliv-root-packet"
      relation: "supports"
      verified_by: ["test-root-family"]
  uncertainty_markers:
    weakest_anchors: ["The top-level root binary may differ materially from the legacy residue family beyond layout and asset expectations."]
    disconfirming_observations: ["Every top-level-only experiment still stalls after resonance start with no tighter explanation than before, or the only callable path requires falling back to the legacy surface."]
---

<objective>
Repair or exact-localize the top-level `/data/local/tmp/dm3_runner` root family
until it is honestly callable or exactly blocked on its own surface.

Purpose: open or close the real gate beneath any official same-family `F2`
handoff.
Output: one retained top-level root-family repair or blocker packet and an
updated `F2` localization route note.
</objective>

<execution_context>
@/Users/prinivenpillay/.codex/get-physics-done/workflows/execute-plan.md
@/Users/prinivenpillay/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/REQUIREMENTS.md
@.gpd/hypotheses/rm10-primary-platform-heterogeneous-learning/HYPOTHESIS.md
@.gpd/phases/01.2.3.4-rm10-validator-rule-repair-f2-root-handoff-and-narrow-heterogeneous-probe/01.2.3.4-CONTEXT.md
@.gpd/phases/01.2.3.4-rm10-validator-rule-repair-f2-root-handoff-and-narrow-heterogeneous-probe/01.2.3.4-RESEARCH.md
@docs/restart/F2_OUTLIER_LOCALIZATION_PLAN.md
@docs/restart/INVOCATION_SURFACE_DOSSIER.md
@artifacts/rm10_f2_surface_probe_20260405T132732Z/OUTCOME.md
@artifacts/rm10_f2_surface_probe_20260405T132732Z/summary.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retain exact top-level root-family layout, binary, and ambient-precondition evidence</name>
  <files>artifacts/rm10_f2_root_repair_*/</files>
  <action>Create a fresh top-level root-family artifact root. Capture top-level and legacy binary hashes, help surfaces, strings, file lookups, cwd assumptions, and the exact layout of `/data/local/tmp` versus `/data/local/tmp/dm3`. Keep comparison evidence from the legacy surface separate from any top-level run evidence.</action>
  <verify>Confirm that the packet distinguishes top-level observations from legacy comparison-only observations and retains every ambient file involved in the current stall envelope.</verify>
  <done>The branch has one exact retained packet describing the top-level family, the legacy comparison surface, and the current ambient-precondition hypotheses without collapsing them together.</done>
</task>

<task type="auto">
  <name>Task 2: Run the bounded top-level-only repair loop until the family is callable or exactly blocked</name>
  <files>artifacts/rm10_f2_root_repair_*/, tools/rm10_engineering_readiness.py, docs/restart/F2_OUTLIER_LOCALIZATION_PLAN.md</files>
  <action>Run bounded experiments on `/data/local/tmp/dm3_runner` only: vary cwd, copied-layout cleanliness, ambient assets, output paths, and any directly evidenced environment or permission preconditions. If a top-level callable CPU row appears, retain the receipt and classify the family as callable. If not, retain the narrowest blocker packet and update the localization note with the exact next admissible move.</action>
  <verify>Reject any path that silently swaps execution onto `/data/local/tmp/dm3/dm3_runner`. Confirm that the final verdict is grounded in top-level evidence only.</verify>
  <done>The branch has either one callable top-level CPU row with receipts or one exact blocker packet that keeps the official `F2` handoff honestly gated.</done>
</task>

</tasks>

<verification>
This plan passes only if the top-level `F2` root family is classified from its
own surface as either callable or exactly blocked with sharper evidence than the
retained `01.2.3.3` probe.
</verification>

<success_criteria>
The branch either opens the top-level `F2` handoff gate honestly or narrows the
blocker far enough that the next engineering move is exact rather than vague.
</success_criteria>

<output>
After completion, create `.gpd/phases/01.2.3.4-rm10-validator-rule-repair-f2-root-handoff-and-narrow-heterogeneous-probe/01.2.3.4-02-SUMMARY.md`.
</output>
