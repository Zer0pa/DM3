# Phase 01.2.3.4 Context

## Locked Decisions

- Start from the retained `01.2.3.3` truth floor only.
- No interim progress narratives, morale reports, or “readiness updates.”
- No claim inflation:
  - explicit-hash success is not default-validator repair
  - legacy `/data/local/tmp/dm3/dm3_runner` success is not top-level root-family
    repair
  - GPU presence is not heterogeneous success
  - NPU remains `ABSTAIN` unless a callable receiptable user-space path exists
- No legacy substitution:
  - the top-level `/data/local/tmp/dm3_runner` family must stand or fail on its
    own surface
- No drift theatre:
  - only produce retained packets, runbooks, blocker notes, and final route
    surfaces that depend on actual receipts

## Mission Order

1. Reverse-engineer the live validator-default rule until it is repaired or
   precisely pinned down.
2. Repair or exact-localize the top-level `F2` root family stall without
   silently switching to the legacy residue surface.
3. Only if the root family becomes callable, run the official same-family `F2`
   outlier handoff.
4. Only then test one narrow heterogeneous split on a preserved observable.
5. Return with retained packets, clean verdicts, and narrowed hypotheses.

## Truth Floor

- `F1` governed Genesis CPU control remains the only trustworthy branch-local
  scientific instrument.
- The live default-validator failure is localized to stale device-side default
  handling.
- The top-level `F2` root family still requires `RegionTags_v1.bin` to clear
  the missing-file gate and still stalls after resonance start even when
  explicit assets are supplied.
- The official `F2` outlier packet is currently governed and blocked.
- Mac Genesis remains the only live `source_built` authority lane.

## Operator Principle

Pattern from physics. Signal from nature.
