---
phase: 01.2.3.4.1.1.3.1-rm10-same-family-width-boundary-environment-engineering-and-replay-ceiling-repair
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/rm10_f2_outlier_capture.py
interactive: false

conventions:
  units: "dimensionless computational units unless protocol says otherwise"
  metric: "not applicable"
  coordinates: "device path identity plus row-set identity"

dimensional_check:
  replay_rows: "[dimensionless]"

approximations:
  - name: "bounded tool extension"
    parameter: "extend the existing outlier capture helper instead of creating a new launcher or controller"
    validity: "valid while the live question is about stronger replay capture on the same surface"
    breaks_when: "the tool silently changes the binary family or the row order semantics"
    check: "the tool must preserve the top-level dm3_runner surface and only add timeout, telemetry, and row-set control"

contract:
  scope:
    question: "What minimum tooling change is required to run the same-family width-boundary replay honestly?"
  claims:
    - id: "claim-width-boundary-tooling"
      statement: "The branch can extend rm10_f2_outlier_capture.py enough to run the exact four-row replay and one bounded follow-up replay without inventing a new execution surface."
      deliverables: ["deliv-tool"]
      acceptance_tests: ["test-width-boundary-tooling"]
      references: ["ref-f2-tool", "ref-readiness-tool", "ref-f2-ledger", "ref-boundary-ledger"]
  deliverables:
    - id: "deliv-tool"
      kind: "code"
      path: "tools/rm10_f2_outlier_capture.py"
      description: "Extended capture tool with configurable row sets, stronger timeout, and periodic telemetry while preserving the same-family surface."
  references:
    - id: "ref-f2-tool"
      kind: "prior_artifact"
      locator: "tools/rm10_f2_outlier_capture.py"
      role: "benchmark"
      why_it_matters: "Current replay capture already preserves row identity and packet custody."
      applies_to: ["claim-width-boundary-tooling"]
      must_surface: true
      required_actions: ["read", "use"]
    - id: "ref-readiness-tool"
      kind: "prior_artifact"
      locator: "tools/rm10_engineering_readiness.py"
      role: "must_consider"
      why_it_matters: "Carries existing live-surface and device preflight logic."
      applies_to: ["claim-width-boundary-tooling"]
      must_surface: true
      required_actions: ["read", "compare", "use"]
    - id: "ref-f2-ledger"
      kind: "prior_artifact"
      locator: "docs/restart/F2_TOPLEVEL_INSTABILITY_LEDGER.md"
      role: "benchmark"
      why_it_matters: "Locks the inherited exact row order and failure localization."
      applies_to: ["claim-width-boundary-tooling"]
      must_surface: true
      required_actions: ["read", "use"]
    - id: "ref-boundary-ledger"
      kind: "prior_artifact"
      locator: "docs/restart/RM10_PERSISTENCE_AND_BOUNDARY_LEDGER.md"
      role: "must_consider"
      why_it_matters: "Keeps the tooling targeted at the width boundary rather than a broader chamber battery."
      applies_to: ["claim-width-boundary-tooling"]
      must_surface: true
      required_actions: ["read", "use"]
  acceptance_tests:
    - id: "test-width-boundary-tooling"
      subject: "claim-width-boundary-tooling"
      kind: "consistency"
      procedure: "Extend the capture tool to accept a configurable row set and stronger timeout/telemetry options while preserving the top-level dm3_runner path and the existing output schema."
      pass_condition: "The branch can run the exact replay and one bounded follow-up replay without a new launcher surface."
      evidence_required: ["deliv-tool", "ref-f2-tool"]
  forbidden_proxies:
    - id: "fp-new-launcher"
      subject: "claim-width-boundary-tooling"
      proxy: "Adding a new opaque launcher or shifting to the legacy /data/local/tmp/dm3/dm3_runner surface"
      reason: "The live question is about the top-level same-family surface."
  links:
    - id: "link-tool"
      source: "claim-width-boundary-tooling"
      target: "deliv-tool"
      relation: "supports"
      verified_by: ["test-width-boundary-tooling"]
  uncertainty_markers:
    weakest_anchors: ["Even stronger capture may still leave the decisive row uninterpretable."]
    disconfirming_observations: ["The tool cannot preserve the same output schema once row-set control is added."]
---

<objective>
Extend the existing same-family replay capture tool just enough to test the
width boundary honestly.

Purpose: support one exact replay and one bounded follow-up replay without
inventing a new surface.
Output: one updated replay capture tool.
</objective>

<execution_context>
@/Users/prinivenpillay/.codex/get-physics-done/workflows/execute-plan.md
@/Users/prinivenpillay/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@tools/rm10_f2_outlier_capture.py
@tools/rm10_engineering_readiness.py
@docs/restart/F2_TOPLEVEL_INSTABILITY_LEDGER.md
@docs/restart/RM10_PERSISTENCE_AND_BOUNDARY_LEDGER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bounded row-set control</name>
  <files>tools/rm10_f2_outlier_capture.py</files>
  <action>Let the existing helper run either the exact four-row replay or a bounded follow-up replay over an explicit subset of rows while keeping the same binary path and row schema.</action>
  <verify>Confirm the tool preserves row identity, artifact layout, and output schema.</verify>
  <done>The phase can compare the full replay and a bounded follow-up replay honestly.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve stronger timeout and periodic telemetry capture</name>
  <files>tools/rm10_f2_outlier_capture.py</files>
  <action>Keep or tighten support for stronger row timeout and periodic telemetry so the decisive cpu_b row is observed under a less artificial ceiling.</action>
  <verify>Run local help and confirm the new flags are explicit.</verify>
  <done>The phase can run the stronger replay envelope it needs.</done>
</task>

</tasks>

<verification>
This plan passes only if the helper stays on the real surface and gains the
minimum control needed for boundary classification.
</verification>

<success_criteria>
The same helper can run both the exact replay and the bounded follow-up replay
without widening the execution family.
</success_criteria>

<output>
After completion, create `.gpd/phases/01.2.3.4.1.1.3.1-rm10-same-family-width-boundary-environment-engineering-and-replay-ceiling-repair/01.2.3.4.1.1.3.1-02-SUMMARY.md`.
</output>
