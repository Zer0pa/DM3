# Plan 03 Summary

Retained `artifacts/phase_01_2_3_4_1_outlier_rerun_20260405T183712Z/` as the
fresh official same-family `F2` rerun packet for `01.2.3.4.1` and wrote
`docs/restart/F2_OFFICIAL_OUTLIER_RERUN_LEDGER.md`. The rerun sharpened the
instability boundary but did not restore a same-family `PASS`: `cpu_a` landed
low, `gpu_a` landed high, `gpu_b` returned low, and the final `cpu_b` row
timed out at `180` seconds and left only a zero-byte device output path. The
correct same-family verdict therefore remains `ABSTAIN`.

```yaml
gpd_return:
  status: completed
  files_written:
    - "artifacts/phase_01_2_3_4_1_outlier_rerun_20260405T183712Z/"
    - "docs/restart/F2_OFFICIAL_OUTLIER_RERUN_LEDGER.md"
    - ".gpd/phases/01.2.3.4.1-rm10-validator-default-rule-same-family-f2-instability-and-observable-gated-replay/01.2.3.4.1-03-SUMMARY.md"
  issues:
    - "The fresh official same-family rerun did not close the CPU anchor because `cpu_b` timed out and produced no retained receipt."
    - "The first two GPU-backed rows still do not define one stable GPU neighborhood; `gpu_a` and `gpu_b` split across the low and high clusters."
  next_actions:
    - "Keep the same-family verdict at `ABSTAIN` and do not reopen heterogeneous work from this packet."
    - "Use the fresh low/high/low-plus-timeout boundary as the exact target for the next same-family replay."
```
