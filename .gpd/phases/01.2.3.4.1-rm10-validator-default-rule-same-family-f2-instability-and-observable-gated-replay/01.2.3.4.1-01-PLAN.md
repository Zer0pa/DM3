---
phase: 01.2.3.4.1-rm10-validator-default-rule-same-family-f2-instability-and-observable-gated-replay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/phase_01_2_3_4_1_validator_default_*/
  - docs/restart/VALIDATOR_DEFAULT_RULE_LEDGER.md
  - tools/rm10_engineering_readiness.py
interactive: false

conventions:
  units: "dimensionless computational units unless protocol says otherwise"
  metric: "not applicable"
  coordinates: "graph / lattice coordinates with explicit boundary and bulk labeling"

dimensional_check:
  validator_rule_tuple: "[dimensionless]"

approximations:
  - name: "bounded validator-default reverse engineering"
    parameter: "live RM10 binary plus retained witness bundles"
    validity: "valid for isolating the default-selection rule without widening into source rebuild or architecture mythology"
    breaks_when: "explicit-hash success is narrated as default repair or a vague stale-pair story survives without exact selector evidence"
    check: "retain binary evidence, selector-path evidence, and default-vs-override behavior together before issuing the verdict"

contract:
  scope:
    question: "What exact default-selection rule or baked-in pair causes the live RM10 validator-default failure, and is the result repaired, precisely localized, or forced into an operator-policy route?"
  claims:
    - id: "claim-validator-default"
      statement: "The live RM10 validator-default failure can be reduced to an exact retained selector rule, stale compiled pair, or deeper validator mismatch rather than vague device-side folklore."
      deliverables: ["deliv-validator-ledger", "deliv-validator-packet"]
      acceptance_tests: ["test-validator-default"]
      references: ["ref-phase-context", "ref-phase-truth-floor", "ref-validator-probe", "ref-gap-ledger", "ref-parity-witness"]
  deliverables:
    - id: "deliv-validator-ledger"
      kind: "report"
      path: "docs/restart/VALIDATOR_DEFAULT_RULE_LEDGER.md"
      description: "Exact ledger of the live RM10 validator-default selector path, comparison matrix, and repaired-or-not verdict."
    - id: "deliv-validator-packet"
      kind: "dataset"
      path: "artifacts/phase_01_2_3_4_1_validator_default_*/"
      description: "Retained validator-default command transcript, binary note, selector-path evidence, comparison matrix, and verdict packet."
  references:
    - id: "ref-phase-context"
      kind: "prior_artifact"
      locator: ".gpd/phases/01.2.3.4.1-rm10-validator-default-rule-same-family-f2-instability-and-observable-gated-replay/01.2.3.4.1-CONTEXT.md"
      role: "definition"
      why_it_matters: "Locks the no-widening and no-proxy rules for this phase."
      applies_to: ["claim-validator-default"]
      must_surface: true
      required_actions: ["read", "use"]
    - id: "ref-phase-truth-floor"
      kind: "prior_artifact"
      locator: ".gpd/phases/01.2.3.4-rm10-validator-rule-repair-f2-root-handoff-and-narrow-heterogeneous-probe/01.2.3.4-04-SUMMARY.md"
      role: "benchmark"
      why_it_matters: "Carries the retained truth that the current gap is a stale compiled pair under default handling."
      applies_to: ["claim-validator-default"]
      must_surface: true
      required_actions: ["read", "use"]
    - id: "ref-validator-probe"
      kind: "prior_artifact"
      locator: "artifacts/rm10_validator_probe_20260405T170530Z/"
      role: "benchmark"
      why_it_matters: "Retains the exact default-vs-explicit witness behavior that must be sharpened or repaired."
      applies_to: ["claim-validator-default"]
      must_surface: true
      required_actions: ["read", "compare", "use"]
    - id: "ref-gap-ledger"
      kind: "prior_artifact"
      locator: "docs/restart/ENGINEERING_GAP_LEDGER.md"
      role: "comparison"
      why_it_matters: "Names the current blocker language that this plan must replace with exact rule evidence."
      applies_to: ["claim-validator-default"]
      must_surface: true
      required_actions: ["read", "compare", "update"]
    - id: "ref-parity-witness"
      kind: "prior_artifact"
      locator: "artifacts/rm10_replay/historical_t1_t7_rm10_parity/"
      role: "benchmark"
      why_it_matters: "The historical parity witness is the shortest clean check against the live selector and compiled default pair."
      applies_to: ["claim-validator-default"]
      must_surface: true
      required_actions: ["compare", "use"]
  acceptance_tests:
    - id: "test-validator-default"
      subject: "claim-validator-default"
      kind: "consistency"
      procedure: "Enumerate the live device binary, build identifier, and embedded reference pair; trace the default-selection path; compare fresh governed bundle, historical parity bundle, explicit-hash override, and default-selection behavior; then classify the result as repaired, precisely localized, or operator-policy-route required."
      pass_condition: "The result names the exact place where default handling fails or is repaired, replaces vague stale-pair wording with exact selector evidence, and never counts explicit-hash override as a default-path repair."
      evidence_required: ["deliv-validator-ledger", "deliv-validator-packet", "ref-validator-probe", "ref-parity-witness"]
  forbidden_proxies:
    - id: "fp-explicit-override"
      subject: "claim-validator-default"
      proxy: "Treating explicit-hash override success as proof that the validator-default path is repaired"
      reason: "The acceptance gate is about the default selector itself."
    - id: "fp-vague-stale-pair"
      subject: "claim-validator-default"
      proxy: "Saying the failure is somewhere in a stale pair without naming the exact selector, embedded tuple, or reference-resolution site"
      reason: "Localization without an exact place is still drift."
  links:
    - id: "link-validator-ledger"
      source: "claim-validator-default"
      target: "deliv-validator-ledger"
      relation: "supports"
      verified_by: ["test-validator-default"]
    - id: "link-validator-packet"
      source: "claim-validator-default"
      target: "deliv-validator-packet"
      relation: "supports"
      verified_by: ["test-validator-default"]
  uncertainty_markers:
    weakest_anchors: ["The live `genesis_cli` is still a prebuilt device binary and may encode a rule that cannot be changed without an external policy decision or replacement build."]
    disconfirming_observations: ["Fresh probes still cannot distinguish stale compiled defaults from a broader validator rule mismatch or reference-directory contamination."]
---

<objective>
Reverse-engineer the live RM10 validator-default rule until it is repaired or
sharply localized to one exact selector or compiled-pair defect.

Purpose: convert the live validator-default blocker from branch folklore into an
exact engineering truth.
Output: a validator-default ledger and retained packet with command transcript,
binary note, selector-path note, comparison matrix, and repaired-or-not
verdict.
</objective>

<execution_context>
@/Users/prinivenpillay/.codex/get-physics-done/workflows/execute-plan.md
@/Users/prinivenpillay/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/REQUIREMENTS.md
@.gpd/phases/01.2.3.4.1-rm10-validator-default-rule-same-family-f2-instability-and-observable-gated-replay/01.2.3.4.1-CONTEXT.md
@.gpd/phases/01.2.3.4-rm10-validator-rule-repair-f2-root-handoff-and-narrow-heterogeneous-probe/01.2.3.4-04-SUMMARY.md
@artifacts/rm10_validator_probe_20260405T170530Z/OUTCOME.md
@artifacts/rm10_validator_probe_20260405T170530Z/identity/embedded_hash_strings.txt
@docs/restart/ENGINEERING_GAP_LEDGER.md
@docs/restart/NEXT_BOUNDED_ENGINEERING_MOVE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retain the live binary, selector path, and default-vs-override evidence</name>
  <files>artifacts/phase_01_2_3_4_1_validator_default_*/</files>
  <action>Create a fresh validator-default artifact root. Retain the live device binary identity, build note, embedded canonical-pair clues, reference-directory behavior, and command transcripts that compare default validation against explicit-hash override on both the fresh governed bundle and the retained historical parity witness.</action>
  <verify>Confirm that every claimed selector or pair clue has a retained receipt and that default handling is kept separate from explicit override handling.</verify>
  <done>The packet contains enough retained evidence to name the exact selector path or compiled pair rather than a vague stale-target story.</done>
</task>

<task type="auto">
  <name>Task 2: Repair or sharply localize the default path and write the ledger</name>
  <files>artifacts/phase_01_2_3_4_1_validator_default_*/, docs/restart/VALIDATOR_DEFAULT_RULE_LEDGER.md, tools/rm10_engineering_readiness.py</files>
  <action>Run the shortest admissible repair or localization loop against the live default path. If the rule can be repaired without cheating, retain the repaired behavior. Otherwise, classify the exact failure site as stale compiled canonical pair, wrong default selector, wrong reference-directory resolution, cwd contamination, environment contamination, or deeper validator mismatch, and write the ledger with a repaired, localized, or operator-policy verdict.</action>
  <verify>Reject any path that still depends on manual explicit hashes while claiming repair. Confirm that the final verdict names the exact place where default handling fails or is repaired.</verify>
  <done>The branch has either a repaired default path or a ledger that precisely fences the defect to one named selector or policy site.</done>
</task>

</tasks>

<verification>
This plan passes only if the validator-default defect is either actually
repaired or reduced to an exact retained selector-rule explanation strong enough
to replace the stale default-pair shorthand.
</verification>

<success_criteria>
The live RM10 validator-default failure is no longer described vaguely. The
branch can point to one exact selector rule, compiled pair, or policy boundary,
with retained evidence.
</success_criteria>

<output>
After completion, create `.gpd/phases/01.2.3.4.1-rm10-validator-default-rule-same-family-f2-instability-and-observable-gated-replay/01.2.3.4.1-01-SUMMARY.md`.
</output>
