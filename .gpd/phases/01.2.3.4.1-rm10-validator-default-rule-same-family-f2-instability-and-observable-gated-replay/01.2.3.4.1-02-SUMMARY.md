# Plan 02 Summary

Retained `artifacts/phase_01_2_3_4_1_f2_toplevel_20260405T183234Z/` as the
fresh top-level same-family `F2` packet for `01.2.3.4.1` and wrote
`docs/restart/F2_TOPLEVEL_INSTABILITY_LEDGER.md`. The top-level root runner is
still callable on `/data/local/tmp/dm3_runner`, but the instability is now
sharply localized inside that callable boundary: the fresh default CPU row
lands on the low cluster (`delta_E=75.2957`, `coherence=0.8776`) while the
explicit-assets top-level CPU row and the cleanroom `RegionTags_v1` row land on
the high cluster (`delta_E≈89`, `coherence≈0.770`). The packet also keeps the
startup dependency truth explicit: `cleanroom_minimal_cpu` still fails on a
missing ambient dependency, and the identity packet now records
`/data/local/tmp/PhonemePatterns_v1.bin` as missing.

```yaml
gpd_return:
  status: completed
  files_written:
    - "artifacts/phase_01_2_3_4_1_f2_toplevel_20260405T183234Z/"
    - "docs/restart/F2_TOPLEVEL_INSTABILITY_LEDGER.md"
    - ".gpd/phases/01.2.3.4.1-rm10-validator-default-rule-same-family-f2-instability-and-observable-gated-replay/01.2.3.4.1-02-SUMMARY.md"
  issues:
    - "The top-level same-family surface is callable but still splits between incompatible low and high CPU states."
    - "The top-level runner is still not hermetic outside cleanroom, and `/data/local/tmp/PhonemePatterns_v1.bin` is currently missing from the retained identity packet."
  next_actions:
    - "Use `F2_TOPLEVEL_INSTABILITY_LEDGER.md` plus `OBSERVABLE_CONTRACT_NOTE.md` as the exact runtime boundary for the official same-family rerun."
    - "Keep the legacy `/data/local/tmp/dm3/dm3_runner` surface fenced as comparison-only."
```
