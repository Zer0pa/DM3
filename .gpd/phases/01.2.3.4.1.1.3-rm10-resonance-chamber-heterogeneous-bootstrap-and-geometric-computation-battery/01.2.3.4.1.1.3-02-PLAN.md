---
phase: 01.2.3.4.1.1.3-rm10-resonance-chamber-heterogeneous-bootstrap-and-geometric-computation-battery
plan: "02"
type: execute
wave: 2
depends_on: ["01.2.3.4.1.1.3-01"]
files_modified:
  - tools/rm10_resonance_chamber.py
  - docs/restart/RM10_GEOMETRIC_ENVIRONMENT_SPEC.md
  - artifacts/phase_01_2_3_4_1_1_3_environment_smoke_*/
interactive: false

conventions:
  units: "dimensionless computational units unless protocol says otherwise"
  metric: "not applicable"
  coordinates: "graph / lattice coordinates with explicit boundary and bulk labeling"

dimensional_check:
  environment_window_state_tuple: "[dimensionless]"

approximations:
  - name: "minimal geometric organism environment"
    parameter: "one workspace root, one output root, persistent state, bounded perturbation windows"
    validity: "valid while state persistence and perturbation scheduling are explicit and receipted"
    breaks_when: "the environment relies on implicit cache state, reused stale outputs, or unlogged mutations"
    check: "build one narrow controller that restores baseline inputs, stamps window identity, and captures pre/post telemetry"

contract:
  scope:
    question: "Can the phase build a minimal persistent environment that supports bounded perturb-and-relax windows on RM10?"
  claims:
    - id: "claim-environment-spec"
      statement: "The phase can define one explicit geometric organism environment for repeated-window RM10 work."
      deliverables: ["deliv-env-spec"]
      acceptance_tests: ["test-env-spec"]
      references: ["ref-context", "ref-research", "ref-helper-reuse"]
    - id: "claim-controller"
      statement: "The phase can implement one narrow repeated-window controller that reuses current capture tooling and proves a clean environment smoke."
      deliverables: ["deliv-controller", "deliv-env-smoke"]
      acceptance_tests: ["test-controller"]
      references: ["ref-rm10-capture", "ref-f2-capture", "ref-lattice-helper"]
  deliverables:
    - id: "deliv-env-spec"
      kind: "report"
      path: "docs/restart/RM10_GEOMETRIC_ENVIRONMENT_SPEC.md"
      description: "Spec for workspace, output root, persistent state, window schedule, perturbation interface, and telemetry policy."
    - id: "deliv-controller"
      kind: "code"
      path: "tools/rm10_resonance_chamber.py"
      description: "Narrow controller for repeated-window resonance batteries."
    - id: "deliv-env-smoke"
      kind: "dataset"
      path: "artifacts/phase_01_2_3_4_1_1_3_environment_smoke_*/"
      description: "Environment smoke packet proving the controller can execute bounded windows with receipts."
  references:
    - id: "ref-context"
      kind: "prior_artifact"
      locator: ".gpd/phases/01.2.3.4.1.1.3-rm10-resonance-chamber-heterogeneous-bootstrap-and-geometric-computation-battery/01.2.3.4.1.1.3-CONTEXT.md"
      role: "definition"
      why_it_matters: "Defines what the phase means by environment and chamber."
      applies_to: ["claim-environment-spec"]
      must_surface: true
      required_actions: ["read", "use"]
    - id: "ref-research"
      kind: "prior_artifact"
      locator: ".gpd/phases/01.2.3.4.1.1.3-rm10-resonance-chamber-heterogeneous-bootstrap-and-geometric-computation-battery/01.2.3.4.1.1.3-RESEARCH.md"
      role: "method"
      why_it_matters: "Defines the repeated-window and persistence-first environment concept."
      applies_to: ["claim-environment-spec", "claim-controller"]
      must_surface: true
      required_actions: ["read", "use"]
    - id: "ref-helper-reuse"
      kind: "prior_artifact"
      locator: "docs/restart/RM10_RESONANCE_HELPER_REUSE_NOTE.md"
      role: "method"
      why_it_matters: "Carries forward the chosen helper baseline."
      applies_to: ["claim-environment-spec", "claim-controller"]
      must_surface: true
      required_actions: ["read", "use"]
    - id: "ref-rm10-capture"
      kind: "prior_artifact"
      locator: "tools/rm10_capture.py"
      role: "method"
      why_it_matters: "Provides current capture patterns and telemetry handling."
      applies_to: ["claim-controller"]
      must_surface: true
      required_actions: ["read", "use", "compare"]
    - id: "ref-f2-capture"
      kind: "prior_artifact"
      locator: "tools/rm10_f2_outlier_capture.py"
      role: "method"
      why_it_matters: "Provides repeated-row capture and F2 telemetry patterns."
      applies_to: ["claim-controller"]
      must_surface: true
      required_actions: ["read", "use", "compare"]
    - id: "ref-lattice-helper"
      kind: "prior_artifact"
      locator: "artifacts/phase_01_2_3_4_1_1_capability_lattice_20260405T204315Z/run_smoke_lattice.py"
      role: "method"
      why_it_matters: "Provides config-restore and repeated-row logic already tested on RM10."
      applies_to: ["claim-controller"]
      must_surface: true
      required_actions: ["read", "use", "compare"]
  acceptance_tests:
    - id: "test-env-spec"
      subject: "claim-environment-spec"
      kind: "consistency"
      procedure: "Specify the minimal environment in enough detail that the later chamber, NPU, and persistence batteries can share one controller and one identity policy."
      pass_condition: "The spec names one workspace root, one output root, explicit state persistence, perturbation schedule, and telemetry contract."
      evidence_required: ["deliv-env-spec", "ref-context", "ref-research"]
    - id: "test-controller"
      subject: "claim-controller"
      kind: "benchmark"
      procedure: "Implement the narrow controller, then execute a clean environment smoke with bounded windows and retained telemetry on the mapped CPU control surface and any mapped accelerator-backed surface that Plan 01 judged callable."
      pass_condition: "The environment smoke proves the controller can restore baseline inputs, stamp window identity, emit one comparable output tuple per window, and explicitly select the mapped CPU surface plus any callable mapped accelerator-backed surface without silent fallback."
      evidence_required: ["deliv-controller", "deliv-env-smoke", "ref-helper-reuse"]
  forbidden_proxies:
    - id: "fp-implicit-environment"
      subject: "claim-environment-spec"
      proxy: "Calling a pile of ad hoc commands an environment without an explicit persistence and perturbation policy"
      reason: "That would make the later chamber result uninterpretable."
    - id: "fp-unlogged-state"
      subject: "claim-controller"
      proxy: "Relying on hidden cache or previous output state without logging or restoring it"
      reason: "That would fake persistence and chamber behavior."
  links:
    - id: "link-env-spec"
      source: "claim-environment-spec"
      target: "deliv-env-spec"
      relation: "supports"
      verified_by: ["test-env-spec"]
    - id: "link-controller"
      source: "claim-controller"
      target: "deliv-controller"
      relation: "supports"
      verified_by: ["test-controller"]
    - id: "link-env-smoke"
      source: "claim-controller"
      target: "deliv-env-smoke"
      relation: "supports"
      verified_by: ["test-controller"]
  uncertainty_markers:
    weakest_anchors: ["Some internal state may still remain opaque inside the binaries even after explicit environment capture."]
    disconfirming_observations: ["The controller cannot run repeated windows without contamination from prior outputs or hidden config mutation."]
---

<objective>
Build the minimal geometric organism environment and prove it runs.

Purpose: create the smallest reusable chamber scaffold for the new phase.
Output: an environment spec, a repeated-window controller, and one environment
smoke packet.
</objective>

<execution_context>
@~/.codex/get-physics-done/workflows/execute-plan.md
@~/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/hypotheses/rm10-primary-platform-heterogeneous-learning/HYPOTHESIS.md
@.gpd/phases/01.2.3.4.1.1.3-rm10-resonance-chamber-heterogeneous-bootstrap-and-geometric-computation-battery/01.2.3.4.1.1.3-CONTEXT.md
@.gpd/phases/01.2.3.4.1.1.3-rm10-resonance-chamber-heterogeneous-bootstrap-and-geometric-computation-battery/01.2.3.4.1.1.3-RESEARCH.md
@docs/restart/RM10_RESONANCE_SURFACE_MAP.md
@docs/restart/RM10_RESONANCE_HELPER_REUSE_NOTE.md
@tools/rm10_capture.py
@tools/rm10_f2_outlier_capture.py
@artifacts/phase_01_2_3_4_1_1_capability_lattice_20260405T204315Z/run_smoke_lattice.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the explicit geometric organism environment spec</name>
  <files>docs/restart/RM10_GEOMETRIC_ENVIRONMENT_SPEC.md</files>
  <action>Turn the new context and research into an explicit environment specification covering workspace root, output root, retained state, baseline restoration rules, perturbation window identity, and required telemetry.</action>
  <verify>Ensure the environment spec is specific enough that later batteries can share one controller and one custody policy without interpretation drift.</verify>
  <done>The environment spec names one exact operating environment for the phase.</done>
</task>

<task type="auto">
  <name>Task 2: Implement a narrow repeated-window controller</name>
  <files>tools/rm10_resonance_chamber.py</files>
  <action>Reuse the existing capture helpers and the prior lattice logic to implement one controller for repeated-window chamber work. Keep the implementation narrow: baseline restore, window stamping, pre/post telemetry capture, short tuple extraction, and explicit surface selection for the mapped CPU and accelerator-backed rows.</action>
  <verify>Confirm the helper does not duplicate the full prior tooling, preserves exact command, cwd, and output identity for each window, and either proves selectable mapped surfaces or records that a mapped surface is not callable.</verify>
  <done>The repo has one controller script suitable for the chamber, NPU, and persistence batteries.</done>
</task>

<task type="auto">
  <name>Task 3: Run an environment smoke and retain the packet</name>
  <files>artifacts/phase_01_2_3_4_1_1_3_environment_smoke_*/, docs/restart/RM10_GEOMETRIC_ENVIRONMENT_SPEC.md</files>
  <action>Use the controller to run the smallest admissible environment smoke, proving that repeated windows execute with explicit identity, restored baseline state, retained telemetry, and explicit surface selection for every row.</action>
  <verify>Reject any smoke that uses stale outputs, hidden state, or unlogged config mutation. Confirm the retained packet contains one comparable tuple per window and names the selected surface for each row.</verify>
  <done>The environment smoke packet proves that the phase can now run bounded windowed experiments honestly.</done>
</task>

</tasks>

<verification>
This plan passes only if the phase has one reusable environment controller and a
smoke packet proving it can run.
</verification>

<success_criteria>
The phase can execute repeated windows on RM10 under one explicit environment
contract.
</success_criteria>

<output>
After completion, create .gpd/phases/01.2.3.4.1.1.3-rm10-resonance-chamber-heterogeneous-bootstrap-and-geometric-computation-battery/01.2.3.4.1.1.3-02-SUMMARY.md.
</output>
