# Phase 01.2.3.4.1.1.3.1.2.1 Context

## Locked Decisions

- This phase exists because the confirmation replay blocked the homeostasis
  gate.
- The first localization question is whether the low regime begins only at row
  three or can already be present at `cpu_a`.
- The same stronger envelope remains locked.
- This phase does not reopen chamber or heterogeneous claims.

## Inherited Truth Floor

- the repaired packet is not reproducible
- the failed confirmation replay kept full-packet callability
- `cpu_a` remained high while `gpu_b` and `cpu_b` moved low

## Operator Mission

1. Run one bounded subset replay that removes `gpu_b` but preserves `cpu_a`,
   `gpu_a`, and `cpu_b`.
2. Decide whether third-row position is the live trigger.
3. Return one exact next move for regime-entrance localization.
