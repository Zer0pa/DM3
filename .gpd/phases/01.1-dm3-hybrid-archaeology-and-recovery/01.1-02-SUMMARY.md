# Phase 01.1-02 Summary

## Objective

Run the smallest honest surviving late-hybrid surface, record the substantive blockers, and classify the GPU residue without inflation.

## Completed Outputs

- added current-pass evidence under `artifacts/dm3_hybrid_archaeology_live_20260403`
- added `authority-pack-dm3-hybrid-20260403/050_GPU_VULKAN_RECOVERY_DOSSIER.md`
- added `authority-pack-dm3-hybrid-20260403/060_PATCH_LOG.md`
- added `authority-pack-dm3-hybrid-20260403/070_RUN_LEDGER.md`

## What Changed

### A bounded executable surface was reproduced now

Fresh host micro-runs were executed from `/Users/Zer0pa/DM3/target/debug/dm3_runner` for:

- `generate_random`
- `generate_random --cpu`
- `generate_tags_v2`
- default inference
- `exp_g2_readout` in inference mode with `DM3_ALLOW_HOST_RUN=1`

All emitted receipts.
All currently collapse to the same normalized `t1_contraction` receipt surface.

### The late-hybrid host surface is executable, but only at smoke level

The fresh reproduction shows:

- bounded launch and receipt proof are real
- task-label diversity is not yet real execution diversity
- `--cpu` versus default does not prove a distinct GPU semantic path on host
- substantive train-mode and `R1R4` probes remain blocked

### The GPU story is now engineering-sized

The current dossier proves:

- real WGSL / `wgpu` residue
- real kernel names
- real fallback logic
- preserved compile drift inside the prototype GPU line

It does not prove:

- fresh governed GPU parity
- fresh RM10 GPU replay

## Carry-Forward Meaning

The late hybrid layer is executable now only as bounded smoke residue.
The current phase does not support a stronger claim than that.

```yaml
gpd_return:
  status: completed
  files_written:
    - "artifacts/dm3_hybrid_archaeology_live_20260403"
    - "authority-pack-dm3-hybrid-20260403/050_GPU_VULKAN_RECOVERY_DOSSIER.md"
    - "authority-pack-dm3-hybrid-20260403/060_PATCH_LOG.md"
    - "authority-pack-dm3-hybrid-20260403/070_RUN_LEDGER.md"
    - ".gpd/phases/01.1-dm3-hybrid-archaeology-and-recovery/01.1-02-SUMMARY.md"
  issues:
    - "This host-focused step was later superseded by a live RM10 rerun, which proved callability but not task-semantic recovery."
    - "Several nominally different host tasks still collapse to the same contraction receipt surface."
    - "Late-hybrid source and runtime assets remain incomplete."
  next_actions:
    - "Execute 01.1-03."
```
