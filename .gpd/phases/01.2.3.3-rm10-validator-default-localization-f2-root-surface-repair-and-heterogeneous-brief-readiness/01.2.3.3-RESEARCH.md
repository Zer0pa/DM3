# Phase 01.2.3.3 Research

## Question

What is the smallest honest phase after `01.2.3.2` that makes this branch
ready for a future heterogeneous-compute brief without inflating `F2` residue
work into governed-family progress?

## Current Evidence

- `F1` remains the only trustworthy branch-local scientific instrument.
- The live governed accelerator bridge on `F1` is closed.
- The retained validator probe on the historical RM10 parity witness now shows
  default validation fail and explicit-hash validation pass.
- The retained `F2` surface probe now shows the top-level
  `/data/local/tmp/dm3_runner` family is blocked by a hidden dependency and
  still stalls after resonance start.
- The legacy `/data/local/tmp/dm3/dm3_runner` remains callable, but it is a
  separate surface.

## Research Conclusions

1. The branch is not gated by hardware attachment. It is gated by validator
   defaults and top-level `F2` root-surface truth.
2. The next honest phase is readiness engineering, not a new science battery.
3. The official `F2` outlier packet must be blocked until the top-level root
   family is proven callable.
4. A future heterogeneous-compute brief is admissible only after the readiness
   gate is retained in artifacts and reflected in startup/state surfaces.
