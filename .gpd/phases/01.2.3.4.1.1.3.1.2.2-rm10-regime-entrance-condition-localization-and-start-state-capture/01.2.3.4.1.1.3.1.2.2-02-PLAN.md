---
phase: "01.2.3.4.1.1.3.1.2.2"
plan: 2
name: "Run the fresh single-row cpu_a start-state battery"
status: planned
---

# Plan 02: Run The Fresh Single-Row cpu_a Start-State Battery

<objective>
Execute at least three cpu_a anchors under controlled pre-run states — deep-clean cold,
shallow-clean post-low, and no-clean immediate rerun — capturing the full pre-state
bundle before each and retaining all packets.
</objective>

## Tasks

### Task 1: Verify device readiness

- Confirm device serial FY25013101C8 still attached
- Confirm binary hash matches: `daaaa84a052b60523bf9d63152f1154225abf119c279aa4b3aabf14487279672`
- Confirm asset hashes match:
  - SriYantraAdj_v1.bin: `22f4bc8fa1858bb98cce445614d498b04292f7c1b54ba2bacbe5786b969f637c`
  - RegionTags_v1.bin: `b5ea6d4c0d10d309a8972d602cdfe1e6b6b776c58d170951ddfa856c08948d6f`
- Confirm PhonemePatterns_v1.bin is absent
- Confirm no dm3_runner process running
- Capture baseline battery and thermal

### Task 2: Anchor A — Deep-clean cold start

1. Execute deep-clean:
   ```
   adb shell 'rm -f /data/local/tmp/*.jsonl'
   ```
2. Verify clean: `adb shell 'ls /data/local/tmp/*.jsonl 2>/dev/null | wc -l'` → 0
3. Wait 120 seconds
4. Capture full pre-state bundle into `artifacts/<run_id>/anchors/A_deep_clean_cold/pre_state/`
5. Execute:
   ```
   adb shell 'cd /data/local/tmp && /system/bin/timeout 420 /data/local/tmp/dm3_runner --mode train --task harmonic --steps 1 --cpu --output /data/local/tmp/entrance_A_cpu_a.jsonl'
   ```
6. Capture post-state and pull receipt into `artifacts/<run_id>/anchors/A_deep_clean_cold/`
7. Record regime classification

### Task 3: Anchor B — No-clean immediate rerun

1. Do NOT clean anything from Anchor A
2. Capture full pre-state bundle (including the Anchor A output still present)
3. Execute immediately with different output name:
   ```
   adb shell 'cd /data/local/tmp && /system/bin/timeout 420 /data/local/tmp/dm3_runner --mode train --task harmonic --steps 1 --cpu --output /data/local/tmp/entrance_B_cpu_a.jsonl'
   ```
4. Capture post-state and pull receipt
5. Record regime classification

### Task 4: Anchor C — Shallow-clean rerun

1. Remove ONLY the target output: `adb shell 'rm -f /data/local/tmp/entrance_C_cpu_a.jsonl'`
2. Do NOT remove Anchor A or B outputs
3. Capture full pre-state bundle
4. Execute:
   ```
   adb shell 'cd /data/local/tmp && /system/bin/timeout 420 /data/local/tmp/dm3_runner --mode train --task harmonic --steps 1 --cpu --output /data/local/tmp/entrance_C_cpu_a.jsonl'
   ```
5. Capture post-state and pull receipt
6. Record regime classification

### Task 5: Anchor D — Deep-clean with short idle (optional, if time permits)

1. Execute deep-clean again: `adb shell 'rm -f /data/local/tmp/*.jsonl /data/local/tmp/entrance_*.jsonl'`
2. Wait only 10 seconds (short idle, NOT long cooldown)
3. Capture full pre-state bundle
4. Execute with fresh output name
5. Record regime classification

Compare: does the idle duration matter, or only the clean state?

### Task 6: Mirror all artifacts to repo

Pull all receipt files, pre/post state captures, and telemetry to the repo artifact
directory. Nothing stays only on-device.

## Deliverables

- `artifacts/<run_id>/anchors/A_deep_clean_cold/` — full packet
- `artifacts/<run_id>/anchors/B_no_clean_immediate/` — full packet
- `artifacts/<run_id>/anchors/C_shallow_clean_rerun/` — full packet
- `artifacts/<run_id>/anchors/D_deep_clean_short_idle/` — full packet (optional)
- `artifacts/<run_id>/identity/` — locked identity bundle

## Success Criteria

- [ ] At least 3 anchors completed with full pre-state capture
- [ ] Each anchor has a clear regime classification (HIGH/LOW) on both axes
- [ ] Binary hash verified unchanged across all anchors
- [ ] All artifacts mirrored to repo
