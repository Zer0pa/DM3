# Phase 01.2.3.2 Context

## Decisions

- The Red Magic 10 Pro remains the primary execution target for this branch. The Mac remains support only.
- This phase is not a proof campaign. It must not promote RM10 presence, GPU visibility, or bundled-residue callability into a narratable win.
- The governed Genesis CPU control family `F1` is the branch anchor.
- The bundled-residue `dm3_runner` train surface `F2` is real, but it currently sits at `feasibility_only`.
- The decisive question for this phase is whether `F2` is only a separate residue-feasibility line or whether any callable accelerator-relevant path can honestly bridge toward `F1`.
- Explicit heterogeneous role partition remains below promotion unless pre-handoff and post-handoff artifacts exist on the same declared family.
- NPU or DSP work remains bounded to one named user-space assist probe. Library or daemon presence alone is not a live lane.
- Do not reopen same-binary `G2` route hunting.
- Do not widen into source reconstruction, wrapper invention, or undeclared redevelopment.

## Required Outputs

- one full identity-and-telemetry anchor packet for a fresh `F1` governed CPU control run
- one full identity-and-telemetry comparison packet for repeated `F2` harmonic CPU and GPU-backed runs
- one explicit family-bridge decision dossier stating `bridge_candidate` or `separate_residue_line`
- one bounded NPU or DSP triage dossier with `PASS`, `FAIL`, `ABSTAIN`, or `BLOCKED`
- one explicit heterogeneous handoff decision note that either preserves `ABSTAIN` or names a real handoff artifact
- one updated 10-document science-and-engineering pack

## Questions To Resolve

1. Does `F1` still reproduce cleanly under current branch governance with retained identity and telemetry?
2. Is `F2` repeatable enough to count as a stable bounded feasibility family rather than a one-off residue effect?
3. Is there any current callable accelerator-preserving surface on `F1`, or is the bridge closed?
4. Can a bounded NPU or DSP assist role be named in advance and receipted honestly?
5. Does any explicit heterogeneous handoff artifact exist, or must the branch continue to abstain?

## Agent Discretion

- Choose repeat count for `F2` within the current thermal envelope, but keep the matrix bounded.
- Reuse existing branch runbooks and governance docs rather than rewriting the whole internal pack.
- Add only the documents needed to answer the phase question decisively and to update the 10-document handoff pack.

## Deferred Ideas

- broad scientific batteries that still lack a common callable observable family
- any claim that `F2` GPU feasibility upgrades `F1`
- any claim that visible QNN, DSP, or thermal sensors prove an NPU lane
- any explicit heterogeneous `PASS` claim without localized handoff artifacts
