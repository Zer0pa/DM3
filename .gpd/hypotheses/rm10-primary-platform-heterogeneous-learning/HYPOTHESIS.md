# Hypothesis: Engineer the Red Magic 10 Pro into the primary DM3 research instrument and treat heterogeneous CPU/GPU/NPU execution as an early first-class learning path

## Description

Engineer the Red Magic 10 Pro into the primary DM3 research instrument and
treat heterogeneous CPU/GPU/NPU execution as an early first-class learning path
rather than a late deployment detail.

## Motivation

<!-- AI-drafted from project context — review and edit -->

Phase `01.2.3` closed the preserved bundled `G2` same-binary surface as
`phase_outcome=PASS` and `route_outcome=FAIL`, so the main line is now pointed
at launcher-adjacency provenance or explicit redevelopment classification. That
is a valid control path, but it also leaves a different question under-tested:
whether the Red Magic itself, treated as the primary engineered instrument
instead of a subordinate witness lane, exposes more decisive structure through
native execution, historical mining, and role-shaped CPU/GPU/NPU probing than a
launcher-first continuation would reveal. The current evidence already shows the
phone is a real execution surface under `/data/local/tmp`, that it has live CPU
and GPU-adjacent infrastructure plus NPU/DSP-adjacent residue, and that the
present trouble is governance and interpretability rather than mere reachability.
This branch exists to test whether an RM10-first engineering programme with
early heterogeneous batteries yields a better falsification surface for DM3 than
the default single-lane, launcher-adjacency-first continuation.

## Expected Outcome

<!-- AI-drafted — review and edit -->

If this hypothesis is right, the branch should produce a governed RM10-primary
execution stack with explicit runbooks, thermal/logging rules, historical clue
indexes, and a battery matrix that can compare CPU-only, GPU-feasibility,
NPU-feasibility, and mixed execution on the same observable families. We expect
one of two decisive outcomes:

1. The RM10 becomes a credible primary instrument with at least one receipted
   observable family where CPU, GPU/NPU feasibility, and heterogeneous role
   partition can be compared without losing interpretability.
2. The branch falsifies the RM10-primary thesis by showing that mixed execution
   destroys determinism, governance, or meaningful observability, in which case
   the main line's caution is strengthened by direct evidence rather than by
   inherited sequencing.

## Success Criteria

<!-- AI-drafted — review and edit -->

- [ ] Branch-local internal engineering, mining, logging, thermal, and battery
      documents exist and are specific enough to govern RM10-first execution
      without leaning on prose-only inheritance.
- [ ] At least one common observable family is exercised on the RM10 with
      explicit CPU baseline, explicit GPU/NPU feasibility verdicts, and an
      explicit heterogeneous pass/fail/abstain outcome under receipts,
      checkpoint identity, and thermal snapshots.
- [ ] The branch returns a merged science-and-engineering package that states
      what the RM10 can honestly do now, what the older histories contributed,
      which observables mattered, whether heterogeneous execution clarified or
      muddied the object, and which next engineering move is most useful.

## Metadata

- **Parent branch:** main
- **Created:** 2026-04-05
- **Status:** Active
- **Current phase:** 01.2.3
