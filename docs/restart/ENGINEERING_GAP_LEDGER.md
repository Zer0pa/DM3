# Engineering Gap Ledger

Last refreshed: `2026-04-16`

## Purpose

Freeze the remaining engineering gates before any official heterogeneous-compute
brief or any cited `F2` outlier-localization packet.

## Current Truth Floor

- Phase `01.2.3.4.1.1.3.1.2.1` is the current completed truth floor.
- Phase `01.2.3.4.1.1.3.1.2.2` is the next planned engineering gate.
- `F1` governed Genesis CPU control remains the only trustworthy branch-local
  scientific instrument.
- The live governed accelerator bridge on `F1` remains closed.
- Governed RM10 validation still depends on `explicit_hash` handling because
  the device default validator remains stale.
- The top-level `/data/local/tmp/dm3_runner` family is callable under the
  stronger same-family envelope.
- The repaired four-row packet did not reproduce.
- Bounded localization showed the low regime can already appear at row `cpu_a`.
- The live `F2` blocker is now entrance-condition selection before row 1.
- NPU remains `ABSTAIN`.
- Explicit heterogeneous role partition remains `ABSTAIN`.
- Mac Genesis remains the only live `source_built` authority lane.
- The validator probe on the historical RM10 parity witness localizes the live
  default-validator failure to a stale compiled device-side default pair, not
  vague bundle corruption.
- No preserved same-family observable currently survives well enough to justify
  a narrow heterogeneous split.
- The legacy `/data/local/tmp/dm3/dm3_runner` remains callable, but it is a
  separate surface and cannot substitute for the top-level root family.

## Prioritized Gaps

| Priority | Gap | Gap class | Why it still matters | Next admissible move |
| --- | --- | --- | --- | --- |
| `P0` | The top-level same-family `F2` surface regime is controlled by internal RNG seeding, not an operator-visible variable | characterization gap | Phase `01.2.3.4.1.1.3.1.2.2` ruled out file cleanup, idle duration, and thermal state as regime selectors. The regime is bistable (HIGH ~57% / LOW ~43%) and biased by binary page cache warmth. A gated warm-up + classification protocol exists. | Follow the gated retry rule in `RM10_ENTRANCE_CONDITION_RETRY_RULE.md`: run a throwaway warm-up, classify the session, and only proceed to the full four-row replay if the classification anchor is HIGH. |
| `P0` | Live RM10 `genesis_cli` default-validator rule remains a stale compiled default pair | provenance gap | Fresh governed work is still interpretable only under `explicit_hash` handling until the actual device-side default rule is repaired or operator-policy-routed. | Keep governed RM10 work under explicit-hash handling and repair or formally route around the embedded stale default pair against the retained witness bundle and live device binary. |
| `P1` | No preserved same-family observable currently supports a narrow heterogeneous split | evidence gap | The branch still lacks one stable same-family observable that survives across sessions honestly. | Keep the narrow heterogeneous verdict at `ABSTAIN` until the entrance-condition work sharpens a legitimate retry rule and the same-family packet reproduces. |
| `P1` | Cleanroom-minimal top-level `F2` still depends on undeclared ambient files | execution gap | The top-level family is callable from `/data/local/tmp`, but the cleanroom-minimal surface still proves there is at least one undeclared ambient dependency beyond the bare binary. | Preserve the top-level working surface as canonical for now and localize the remaining ambient dependency without confusing it with the legacy family. |
| `P2` | Fresh RM10 Genesis lanes remain non-source-built | provenance gap | Fresh executability is still real but not source parity, and the branch must not drift into source-built language. | Preserve `prebuilt_stub` / `mixed_prebuilt_backed` labels until true source-built parity exists. |
| `P2` | NPU and explicit heterogeneous remain unopened | evidence gap | No same-family handoff packet exists and no receiptable NPU assist path exists. | Keep both at `ABSTAIN` until a real same-family stage split or assist path is retained. |

## Explicitly Closed Or Fenced Items

- Closed: the live governed accelerator bridge on `F1`
- Fenced: `F2` as residue-family engineering and classification work only
- Fenced: fresh RM10 Genesis lanes as non-sovereign and non-source-built
- Fenced: NPU and explicit heterogeneous work at `ABSTAIN`

## What Not To Reopen Casually

- Do not reopen bridge language from residue-only `F2` work.
- Do not treat explicit-hash success as repaired default validation.
- Do not substitute legacy `/data/local/tmp/dm3/dm3_runner` callability for the
  top-level root family.
- Do not narrate a callable four-row packet as a reproducible same-family win.
- Do not narrate the existence of the official same-family `F2` packet as a
  heterogeneous success by itself.
- Do not narrate a narrow heterogeneous split from one drifting GPU row, from
  residue-only GPU markers, or from legacy substitution.
- Do not treat startup readiness prose as equivalent to a retained readiness
  packet.
