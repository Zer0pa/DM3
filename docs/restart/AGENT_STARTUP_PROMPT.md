# DM3 RM10 Branch Startup Prompt

Last refreshed: `2026-04-16`

Copy the block below into a new thread.

---

You are taking over the DM3 RM10-primary hypothesis branch in:

- `/Users/Zer0pa/DM3/restart-hypothesis-rm10-primary-platform`

Read first, in order:

1. `/Users/Zer0pa/DM3/AGENTS.md`
2. `/Users/Zer0pa/DM3/restart-hypothesis-rm10-primary-platform/.gpd/STATE.md`
3. `/Users/Zer0pa/DM3/restart-hypothesis-rm10-primary-platform/.gpd/PROJECT.md`
4. `/Users/Zer0pa/DM3/restart-hypothesis-rm10-primary-platform/.gpd/ROADMAP.md`
5. `/Users/Zer0pa/DM3/restart-hypothesis-rm10-primary-platform/docs/restart/RM10_AGENT_HANDOVER_20260416.md`
6. `/Users/Zer0pa/DM3/restart-hypothesis-rm10-primary-platform/docs/restart/STARTUP_READING_ORDER_FREEZE.md`

Treat the governing objective as sovereign:

- do not reward hack
- do not narrate a local improvement as the branch win
- do not reopen pass language from mixed evidence
- keep `F1`, top-level `F2`, and legacy bundled archaeology separate
- keep geometry sovereign and transformer claims thin
- treat environment as part of the computation
- treat thermal/runtime signals as confound screens only

Current live branch truth:

- last completed phase: `01.2.3.4.1.1.3.1.2.1`
- next planned phase: `01.2.3.4.1.1.3.1.2.2`
- `F1` governed Genesis CPU control remains the only trustworthy branch-local
  scientific instrument
- the live RM10 default validator is still stale and governed `F1` work remains
  under `explicit_hash` handling
- top-level `/data/local/tmp/dm3_runner` is callable under the stronger
  envelope, but the full four-row repair did not reproduce
- bounded localization showed the low regime can already appear at `cpu_a`
- the live blocker is entrance-condition selection before row 1
- homeostasis remains blocked
- NPU remains `ABSTAIN / inventory_only`
- explicit heterogeneous role partition remains `ABSTAIN`

Red Magic device facts to re-verify immediately:

- serial: `FY25013101C8`
- model: `NX789J`
- governed `F1` lane:
  `/data/local/tmp/genesis_cli` from `/data/local/tmp/SoC_runtime/workspace`
- top-level same-family `F2` lane:
  `/data/local/tmp/dm3_runner` from `/data/local/tmp`
- legacy archaeology lane:
  `/data/local/tmp/dm3/dm3_runner` from `/data/local/tmp/dm3`

Run these before planning or execution:

- `adb devices -l`
- `adb -s FY25013101C8 shell getprop ro.product.model`
- `adb -s FY25013101C8 shell getprop ro.hardware.vulkan`
- `adb -s FY25013101C8 shell getprop ro.hardware.egl`
- `adb -s FY25013101C8 shell 'ls -ld /data/local/tmp /data/local/tmp/SoC_runtime/workspace /data/local/tmp/dm3 /data/local/tmp/genesis_cli /data/local/tmp/dm3_runner /data/local/tmp/dm3/dm3_runner'`
- `adb -s FY25013101C8 shell dumpsys battery`
- `adb -s FY25013101C8 shell dumpsys thermalservice`

Use these skills first:

1. [$gpd-resume-work](/Users/prinivenpillay/.agents/skills/gpd-resume-work/SKILL.md)
2. [$gpd-health](/Users/prinivenpillay/.agents/skills/gpd-health/SKILL.md)
3. [$gpd-progress](/Users/prinivenpillay/.agents/skills/gpd-progress/SKILL.md)
4. [$gpd-plan-phase](/Users/prinivenpillay/.agents/skills/gpd-plan-phase/SKILL.md)
5. [$gpd-execute-phase](/Users/prinivenpillay/.agents/skills/gpd-execute-phase/SKILL.md)

Your immediate mission is not to rerun the full four-row packet.
Your immediate mission is to continue from exactly this point:

- formalize and execute `01.2.3.4.1.1.3.1.2.2`
- freeze a start-state capture contract for single-row `cpu_a` anchors
- compare cold / cleaned starts against post-session starts
- localize the strongest candidate for the high-vs-low regime selector
- write the exact rule for when a full four-row confirmation replay may reopen

Minimum battery:

1. `cpu_a` after explicit cleanup plus long idle / cooldown
2. `cpu_a` immediately after a retained low-regime packet
3. `cpu_a` immediately after a retained high-regime packet, if a high packet
   can be re-entered honestly

Retain before each anchor:

- battery snapshot
- thermal snapshot
- `/proc/loadavg`
- `dm3_runner` process snapshot
- binary hashes
- sidecar listing

Do not do any of the following unless the entrance-condition gate is actually
cleared:

- do not reopen homeostasis
- do not widen chamber-science language
- do not substitute legacy `/data/local/tmp/dm3/dm3_runner` for the top-level
  `/data/local/tmp/dm3_runner`
- do not treat `explicit_hash` as validator repair
- do not infer signal from heat, runtime, or GPU presence
- do not reopen NPU or explicit heterogeneous claims

The one question that should dominate the next session is:

`what selects the high versus low same-family regime before row cpu_a starts?`

---
