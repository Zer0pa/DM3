# RM10 Same-Family Handoff Truth Ledger

Plan: `01.2.3.4.1.1-04`

Classification: `explicit abstain`

Boundary family considered:
official same-family `F2` top-level `/data/local/tmp/dm3_runner`
CPU/GPU/GPU/CPU bracket only

Status: `battery not admissible`

## Gate Input

- `docs/restart/RM10_PROPERTY_MAP.md` closed Wave 3 as an explicit no-signal
  stop.
- The cleaned `F1` smoke lattice retained exactly one semantic receipt family
  across all six rows.
- Phase context says Battery D runs only if Batteries B and C justify it.
- Wave 3 summary already states that Wave 4 may proceed only on the abstain
  path unless a later battery surfaces a real mapped property.

## Missing Carry Property

No mapped property exists that is both:

- measurable on both sides of one declared same-family boundary
- distinct from receipt emission, runtime length, thermal steadiness, config
  echo, or legacy-surface substitution

Explicitly absent on current evidence:

- thresholds or mode flips
- persistence of a changed response
- hysteresis
- recovery after perturbation
- boundary or window sensitivity
- packet-format sensitivity

## Why The Real Battery Stays Closed

1. Wave 3 produced no carryable property on the cleaned discovery lane. Running
   Battery D now would test handoff existence without a property, which would
   violate the plan's property-first contract.
2. `docs/restart/F2_OFFICIAL_OUTLIER_RERUN_LEDGER.md` does not reopen the
   gate. Its retained official same-family rerun is itself `ABSTAIN` and
   localizes only a low/high/low sequence followed by lost `cpu_b` custody.
3. `docs/restart/F2_OUTLIER_LOCALIZATION_PLAN.md` and
   `tools/rm10_f2_outlier_capture.py` preserve the official same-family
   boundary on `/data/local/tmp/dm3_runner` and explicitly fence the legacy
   `/data/local/tmp/dm3/dm3_runner` path as an alternate binary that is noted
   but not used.
4. Using same-family output emission as the tested property would reward
   `fp-handoff-exists`. Using the legacy residue surface to rescue observability
   would violate `fp-legacy-substitution`.

## Boundary Note

- If Battery D ever reopens, the same-family boundary remains the official
  top-level `/data/local/tmp/dm3_runner` family under locked identity.
- No same-family handoff packet was created in this plan because the
  admissibility gate never opened.

## Reopen Conditions

Reopen Battery D only if all of the following become true first:

1. A later smoke and property cycle on the cleaned lane surfaces a second
   retained semantic family and names one concrete anchor property.
2. That property is measurable both pre-handoff and post-handoff under one
   declared same-family boundary without collapsing to config echo, runtime,
   thermal theatre, or receipt-existence.
3. The official same-family surface preserves enough custody to retain a
   pre-boundary artifact, the boundary note, and a post-boundary artifact on
   the same declared family.

## Verdict

Verdict: `ABSTAIN`

Why:

- Battery D is gated by a property that does not exist on current evidence.
- The official same-family `F2` truth floor remains a boundary-localization
  abstain, not a preserved property witness.
- The honest result is no admissible same-family handoff test, not a forced
  same-family battery.
