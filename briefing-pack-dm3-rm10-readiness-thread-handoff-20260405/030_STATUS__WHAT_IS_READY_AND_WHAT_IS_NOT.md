# Current Status

## Ready Now

- the repo is stable enough to brief from
- the branch has a canonical live readiness phase
- the science engineering team can discuss the real gate without ambiguity
- the validator-default issue is localized enough to talk about concretely
- the top-level `F2` root-surface issue is localized enough to talk about
  concretely
- the official `F2` outlier entrypoint is now governance-safe

## Not Ready Yet

- repaired default validation on the live RM10 `genesis_cli`
- a callable top-level `/data/local/tmp/dm3_runner` root family
- an admissible official `F2` outlier packet
- any explicit heterogeneous-compute success claim
- any NPU assist claim beyond `ABSTAIN`
- any same-family handoff artifact

## Current Honest Scientific Ceiling

- `F1` governed Genesis CPU control remains the only trustworthy branch-local
  scientific instrument
- the live governed accelerator bridge on `F1` remains closed
- `F2` remains residue-only and `unstable_feasibility`
- the official `F2` outlier packet is currently blocked
- NPU remains `ABSTAIN`
- explicit heterogeneous role partition remains `ABSTAIN`
- Mac Genesis remains the only live `source_built` authority lane

## Delivery State

- `git diff --check` is clean
- `state.json` parses
- touched Python tools compile under `python3 -m py_compile`
- changes are local and uncommitted
