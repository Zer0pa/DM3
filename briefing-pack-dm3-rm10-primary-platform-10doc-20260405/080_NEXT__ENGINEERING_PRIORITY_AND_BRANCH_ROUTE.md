# Next

Most useful next engineering move:

1. keep the governed Genesis CPU control family as the branch anchor
2. stabilize and repeat the bundled-residue harmonic CPU versus GPU family with a full identity packet and telemetry capture
3. decide explicitly whether that harmonic family is only a residue-feasibility line or a bridge toward the primary governed family
4. attempt NPU or DSP assist only if a receiptable user-space CLI or wrapper can be named in advance
5. reopen explicit heterogeneous comparison only after a handoff manifest exists

Do not do next:

- reopen same-binary bundled `G2` route hunting
- narrate accelerator visibility into readiness
- let `F2` feasibility success silently upgrade `F1`
- widen into redevelopment without naming it
