# Next

Most useful next engineering move:

1. keep the current CPU control observable as the branch anchor
2. find the smallest GPU derivative that preserves that same observable
3. if that fails, attempt the narrowest bounded NPU assist probe with the same
   logging discipline
4. only then reopen heterogeneous comparison

Do not do next:

- reopen same-binary bundled `G2` route hunting
- narrate accelerator visibility into readiness
- widen into redevelopment without naming it
